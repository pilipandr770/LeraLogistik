# Alembic migrations live here.
#
# Generate the first migration after you have a running Postgres:
#
#     alembic revision --autogenerate -m "initial schema"
#     alembic upgrade head
#
# The autogenerate command reads app/db/models.py (via migrations/env.py)
# and compares against your live DB, producing a file in this directory.
